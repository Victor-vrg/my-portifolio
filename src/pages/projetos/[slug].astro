---
import { type CollectionEntry, getCollection } from 'astro:content';

import BaseLayout from '../../layouts/Layout.astro';
import Hero from '../../components/Hero.astro';
import Icon from '../../components/Icon.astro';
import Pill from '../../components/Pill.astro';
import { render } from 'astro:content';
import '../../styles/markdown.css';
import ScrollReveal from '../../components/ScrollReveal.astro';

interface Props {
  entry: CollectionEntry<'projetos'>;
}

export async function getStaticPaths() {
  const projetos = await getCollection('projetos');
  return projetos.map((entry) => ({
    params: { slug: entry.slug},
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title} description={entry.data.description}>
  <div class="stack gap-20">
    <div class="stack gap-15">
      <header>
        <div class="wrapper stack gap-2">
          <div class="breadcrumbs flex items-center">
            <a class="back-link" href="/projetos/"> <Icon icon="arrow-left" /></a>
          <a class="back-link pl-4 text-xl" href="/projetos/"> Projetos</a>
          
        </div>
          <Hero title={entry.data.title} align="center">
            <div class="shrink-0 bg-border h-[1px] w-full my-4 bg-slate-500"></div>
            <div class="details">
              <div class="flex gap-2 tags pb-4 justify-center">
                {entry.data.tags.map((tag) => <Pill>{tag}</Pill>)}
              </div>
              <div class="flex flex-row columns-2 items-center">
              <p class="description ">{entry.data.description}</p>
              {entry.data.thumbnail && (
                <img src={entry.data.thumbnail} alt={entry.data.title} width="600" height="300" />
              )}
              </div>
              <a href={entry.data.liveUrl} class="liveUrl size-1">Clique aqui para ver!</a>
            </div>
          </Hero>
        </div>
      </header>
      <main class="wrapper">
        <div class="stack gap-10 content">
          <div class="content markdown-content">
            <ScrollReveal>
            <Content />
          </ScrollReveal>
          </div>
        </div>
      </main>
    </div>
   
  </div>
</BaseLayout>

